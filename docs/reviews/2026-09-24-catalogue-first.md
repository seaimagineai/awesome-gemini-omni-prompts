# Catalogue-first layout for experienced users — 2026-09-24

The user clarified that readers already understand the model and prompting. The homepage now prioritizes categorization, browsing examples and results, and copying complete prompts. This supersedes the tutorial-first source layout recorded earlier.

All 15 language homepages now present a seven-category index, six illustrated full prompts and ten attributed external result references (four official, six community). Each illustrated example links to its input image, category and plain-text prompt. The former teaching sections are preserved in linked locale guides under `docs/guides/`.

The 60-case index links directly to stable anchors in all seven collection pages. Every case has a TXT export; a combined file includes all 60 recipes. Multi-part recipes retain all 77 original fenced prompt blocks in order and distinguish their individual parts. These are practice briefs, not individually verified outputs. The six reference images remain clearly labeled inputs. Google and community examples retain model-version, source and verification boundaries.

Independent review checked actual English/Chinese pages, case anchors and multi-part text exports. The reviewer requested visible FxTwitter/native-playback limits and the full official model name; these were accepted and corrected across all 15 languages. The reviewer then reported no required fixes.

The main agent independently checked every prior README code block remains in its new homepage or linked guide; original collection block hashes remain unchanged; exports include each case's full content; relative links resolve; and all three generators are deterministic. GitHub Markdown rendering of English, Chinese, Japanese and Arabic confirmed six code blocks, three tables and thirteen image elements on each homepage. The index rendered seven category tables, and the editing collection retained all eighteen code blocks. These checks verify documentation, not model generation quality or native X video playback.
