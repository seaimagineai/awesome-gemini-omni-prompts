# Distinct category artwork and nine copyable examples

The user requested distinct artwork between the category matrix and the full-width examples, then expanded the latter from six to nine. The six existing full-width examples are retained. Six replacement category concepts depict porcelain-kiln cinema, fashion styling, tide-pool wildlife, clay animation, patterned kinetic sand and a keyboard food kiosk. The other three category concepts remain distinct.

An independent reviewer inspected all six replacement images and their category mappings, confirming category relevance and no repeated subjects with the retained or planned featured examples. The locale reviewer completed nine titles and corrected the introduction, section label and preview note in all 15 languages. The catalogue remains 76 recipes; the homepage features nine editorial examples, not nine newly tested model results.

The validator now rejects shared image paths and identical image content between the category and featured sections, checks every featured TXT export against its source prompt, and checks all artwork against its provenance hash.

The final independent reviewer inspected the three added first frames against their complete prompts and reported no conflicts: the bookshop starts rainy with lamps already lit, the meteorite is physically supported and stationary, and the robot holds one capsule away from the empty unlit socket. The main agent corrected the streetlamp wording and removed an ambiguity that could have changed the orbit into an overhead shot.

Final main-agent checks: all nine featured images load and span the full 916-pixel content width in the browser preview; the category and public-result matrices retain full-width layouts. English, Chinese, Japanese and Arabic rendered through GitHub Markdown with ten code blocks (nine examples plus one short editing template). All 15 locales have nine titles and matching downloads. Generation is idempotent, 1,736 relative links pass validation, original source recipes and the original six featured image/prompt pairs are unchanged. These checks do not constitute model-output testing.
